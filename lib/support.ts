/**
 * The address we show users. It's a plain mailto: target — their mail client
 * sends straight here, nothing routes through Resend — so it has to be a real
 * inbox rather than an alias that needs forwarding set up somewhere.
 */
export const SUPPORT_EMAIL = "info@prodraw.ai";
